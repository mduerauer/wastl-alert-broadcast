# The libraries to build
noinst_LIBRARIES = libwab.a
noinst_HEADERS = libwab.h

# The files to add to the library and to the source distribution
libwab_a_SOURCES = \
                         $(libwab_a_HEADERS) \
                         libwab.c
